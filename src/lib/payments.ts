// Example using Paguelofacil (update with actual keys)
export const createPayment = async (paymentData: any) => {
  // send request to Paguelofacil API
  console.log('Payment data:', paymentData)
  // integrate fetch call to Paguelofacil
}

